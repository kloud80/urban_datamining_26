# 작업 규칙 — urban_datamining_26

이 저장소에서 슬라이드·노트북을 만들거나 고칠 때 지킬 것. 판단이 갈리면
`docs/제작_가이드라인.md` 가 우선한다.

## 이 저장소가 담는 것

**산출물만 담는다.** 학생이 열어 보는 것 — 완성된 강의안(HTML·PPTX), 실습
노트북, 그리고 그 둘로 가는 링크가 있는 `README.md`. 슬라이드 빌드 도구와 덱
원본(`*.dc.html`)은 상위 작업 폴더에 있고 여기 올리지 않는다.

```
README.md          학생 진입점 — 주차별 [강의안 io] · [노트북 Colab]
index.html         주차 목록 랜딩 (github.io 첫 화면)
CLAUDE.md          이 파일
docs/
  제작_가이드라인.md   규격·패턴·체크리스트의 원본
  16주_매핑.md         주차 ↔ 노트북 ↔ 상태 ↔ 미해결 갭
W<nn>_<주제>/
  HARNESS.md       그 주차의 작업지시서 — 여기부터 읽는다
  slides/          완성된 강의안과 그 자산
  notebooks/       실습 노트북
_미배정/            주차에 아직 못 붙인 25년 자산
```

## 한 주차를 만들 때

1. **`W<nn>_*/HARNESS.md` 를 먼저 읽는다.** 그 주차의 오늘의 질문, 가진 자료,
   165분 배분, 미해결 갭이 거기 정리돼 있다.
2. 노트북의 마크다운 셀 제목이 곧 절 구분이다. 슬라이드 순서를 **텍스트로 먼저**
   적고 패턴 코드(P1~P8)를 붙인 뒤에 만든다.
3. 만들고 나면 HARNESS.md 의 체크박스를 실제로 확인하며 채운다.

## 절대 어기지 않을 것

- **모든 슬라이드 노트란에 출처를 남긴다** — `04 CNN/14 건물외벽학습.ipynb · cell 12`
  형식. 내년 개편이 전적으로 여기에 달려 있다.
- **이미지는 원본 px ÷ 150 이 배치 폭(in)의 상한이다.** 넘겨서 키우면 강의실
  화면에서 뭉갠다. 800px 미만(C등급)은 그대로 쓰지 말고 다시 만든다.
- **본문 14pt 미만을 쓰지 않는다.** 뒷자리에서 안 읽힌다.
- **실존 인물의 얼굴을 생성하지 않는다.** 튜링·로젠블랫 등은 퍼블릭 도메인
  원본을 받아 쓴다. 생성하면 없는 얼굴을 역사적 인물이라며 강의에서 제시하게 된다.
- **데이터를 담은 그림은 이미지 모델로 만들지 않는다.** 산점도·정확도 표처럼
  값이 있는 그림은 차트로 그린다. 이미지 모델은 점을 지어내고 축 라벨을 틀린다.
- **공개되지 않은 수치를 그리지 않는다.** 프런티어 모델의 파라미터 수처럼
  발표되지 않은 값은 추정치로 채우지 말고 비워 둔다.

## 링크는 손으로 적지 않는다

`README.md` 와 `index.html` 의 링크 60여 개는 상위 작업 폴더의
`.dcpull/gen_2026_docs.py` 가 **디스크에 실제로 있는 파일을 훑어** 만든다.
파일을 옮기거나 추가했으면 그 스크립트를 다시 돌린다. 손으로 고치면 다음
생성 때 덮어써지고, 그 사이에 틀린 링크가 학생에게 나간다.

주차 구성 자체를 바꿀 때는 `.dcpull/scaffold_2026.py` 의 `WEEKS` 를 고친다.
그것이 폴더·노트북 배치·README·HARNESS 전부의 단일 출처다.

## 아직 안 정한 것 (막히면 여기부터)

`docs/제작_가이드라인.md` 3절의 G1~G6 중 살아 있는 것:

- **G2** 11~14주(프롬프트·검색·에이전트·하네스)는 25년 자산이 사실상 없다.
  제작 공수의 절반 이상이 여기다.
- **G5** `hycms.hanyang.ac.kr` 링크를 쓰는 노트북 **21개**의 유효성을 개강 전
  일괄 확인해야 한다. OpenAI 키가 필요한 노트북은 **4개**(28·30·31·32).
- **G6** 13주차 "Open Claw" 가 무엇인지 확정해야 실습 스택이 정해진다.
